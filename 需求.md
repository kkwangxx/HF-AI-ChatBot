> 你现在接手的是一个已经完成第一版的 Python AI Chat 项目。
>
> ## 一、非常重要：不要重写现有 Chat
>
> 当前项目已经可以：
>
> - 打开聊天页面
> - 输入消息
> - 调用 AI API
> - 使用 API Key
> - 获取 AI 回复
> - 可能已经支持 SSE 流式输出
>
> **第一步必须先完整分析当前项目。**
>
> 不要重新创建一个 Chat 项目。
>
> 不要替换现有 UI。
>
> 不要删除现有功能。
>
> 在现有项目基础上，将它逐步升级为：
>
> # MOM AI Agent
>
> 目标：
>
> 让用户在使用 MOM 系统过程中，可以直接向 AI 助手询问：
>
> - 系统业务流程
> - 功能操作步骤
> - 操作之后会发生什么
> - 为什么不能执行某个操作
> - 某个功能有哪些校验规则
> - 某个业务规则在代码哪里实现
> - 某个按钮调用什么接口
> - 某个业务数据为什么是这个状态
> - 当前单据为什么操作失败
>
> AI 必须尽量基于 MOM 项目的真实代码、业务文档和业务数据回答，而不是凭经验猜测。
>
> ------
>
> # 二、总体架构
>
> 当前：
>
> ```text
> 用户
>  ↓
> Chat UI
>  ↓
> Python
>  ↓
> AI API
> ```
>
> 升级为：
>
> ```text
> 用户
>  ↓
> Chat UI
>  ↓
> FastAPI
>  ↓
> MOM AI Agent
>  ↓
> LLM
>  ↓
> Agent Tools
>  ├── 业务知识搜索
>  ├── 操作流程搜索
>  ├── MOM 代码搜索
>  ├── 代码文件读取
>  ├── 数据库查询
>  └── 当前页面/业务对象上下文
> ```
>
> Agent 根据用户问题自行判断需要调用哪些工具。
>
> ------
>
> # 三、第一步：分析现有项目
>
> 现在先不要修改代码。
>
> 先扫描整个当前项目，重点确认：
>
> 1. Python 版本
> 2. FastAPI / Flask / Django
> 3. 当前 AI SDK
> 4. API Key 如何配置
> 5. AI API 请求在哪里
> 6. SSE 流式输出在哪里
> 7. Chat UI 如何实现
> 8. 消息数据结构
> 9. Session / Conversation 如何实现
> 10. 环境变量如何配置
> 11. requirements.txt / pyproject.toml
> 12. 当前项目启动方式
>
> 分析完成后，再开始改造。
>
> ------
>
> # 四、保留现有 AI API
>
> 当前项目已经通过 API Key 调用 AI。
>
> 不要更换 AI 服务。
>
> 不要把 API Key 暴露到前端。
>
> 继续使用：
>
> ```text
> .env
> ```
>
> 例如：
>
> ```env
> AI_API_KEY=xxxx
> AI_API_BASE_URL=https://xxx/v1
> AI_MODEL=xxx
> ```
>
> 具体变量名以当前项目已有配置为准。
>
> 如果当前项目已经有成熟的 AI Client：
>
> **优先复用。**
>
> 不要重复创建第二套 AI Client。
>
> ------
>
> # 五、第一阶段不要做复杂 RAG
>
> 这一阶段：
>
> **不要先引入 Milvus、Elasticsearch、向量数据库、Embedding、Rerank 等复杂组件。**
>
> 先实现：
>
> ```text
> Agent
> +
> Tools
> +
> 关键词/文本检索
> +
> 业务文档
> +
> 代码搜索
> +
> 数据库查询
> ```
>
> 等 Agent 基础能力跑通以后，再根据实际检索效果决定是否增加 RAG。
>
> ------
>
> # 六、建立 MOM 知识目录
>
> 在项目中增加一个 MOM 知识目录。
>
> 建议：
>
> ```text
> mom_knowledge/
> ├── business/
> │   ├── production/
> │   ├── work_order/
> │   ├── report/
> │   ├── picking/
> │   ├── return/
> │   ├── quality/
> │   └── inventory/
> │
> ├── operation/
> │   ├── production/
> │   ├── work_order/
> │   ├── report/
> │   ├── picking/
> │   └── inventory/
> │
> ├── rules/
> │   ├── production/
> │   ├── work_order/
> │   ├── picking/
> │   └── inventory/
> │
> └── glossary.md
> ```
>
> 第一阶段如果实际 MOM 项目文档不存在，不要凭空生成大量业务规则。
>
> 可以创建：
>
> ```text
> README.md
> ```
>
> 说明以后业务人员/开发人员应该如何补充知识。
>
> ------
>
> # 七、业务知识的目标
>
> 业务知识用于回答：
>
> ### 操作问题
>
> 例如：
>
> ```text
> 生产工单怎么创建？
> ```
>
> 回答：
>
> ```text
> 进入生产管理 → 生产工单
> 点击新增
> 选择生产订单
> 填写相关信息
> 保存
> 点击投产
> ```
>
> ------
>
> ### 流程问题
>
> 例如：
>
> ```text
> 生产工单创建以后下一步是什么？
> ```
>
> 回答：
>
> ```text
> 生产工单
>  ↓
> 投产
>  ↓
> 开工
>  ↓
> 报工
>  ↓
> 质检
>  ↓
> 完工审核
>  ↓
> 成品入库
> ```
>
> 具体流程必须以 MOM 实际业务资料/代码为依据。
>
> ------
>
> ### 业务规则
>
> 例如：
>
> ```text
> 一个工单能不能领料两次？
> ```
>
> 如果实际业务规则是：
>
> ```text
> 一个工单只能执行一次领料
> ```
>
> 则回答。
>
> 如果无法从知识或代码确认：
>
> 明确告诉用户：
>
> ```text
> 当前项目资料中没有找到足够依据，无法确认该规则。
> ```
>
> **绝对不要编造业务规则。**
>
> ------
>
> # 八、代码检索工具
>
> 增加：
>
> ```text
> search_code
> ```
>
> 作用：
>
> 在 MOM 项目代码目录中搜索：
>
> - 类名
> - 方法名
> - 字段名
> - API
> - 错误提示
> - 中文业务关键词
> - SQL
> - 表名
> - 注解
>
> 例如用户问：
>
> ```text
> 报工数量不能超过工单数量的校验在哪里？
> ```
>
> Agent 可以搜索：
>
> ```text
> 报工数量
> 工单数量
> reportQty
> orderQty
> quantity
> ```
>
> 返回匹配文件、行号、代码上下文。
>
> ------
>
> # 九、代码读取工具
>
> 增加：
>
> ```text
> read_code_file
> ```
>
> 输入：
>
> ```text
> 文件路径
> ```
>
> 可以读取指定代码文件。
>
> 但是必须限制访问范围。
>
> 例如允许：
>
> ```text
> /mom/src/**
> ```
>
> 禁止：
>
> ```text
> .git
> .env
> application-prod.yml
> application-production.yml
> secret
> password
> token
> api-key
> ```
>
> 不要把整个 MOM 项目一次性塞给 LLM。
>
> 只有 Agent 判断需要时才读取相关文件。
>
> ------
>
> # 十、代码搜索不要只支持文件名
>
> 搜索应该支持：
>
> ```text
> class
> method
> field
> annotation
> error message
> table name
> API path
> Chinese keyword
> English keyword
> ```
>
> 例如：
>
> ```text
> “领料失败”
> ```
>
> 应该能够找到：
>
> ```text
> throw new BusinessException("该工单已经领料");
> ```
>
> 然后进一步读取上下文。
>
> ------
>
> # 十一、数据库查询工具
>
> 增加：
>
> ```text
> query_database
> ```
>
> 第一阶段：
>
> **只允许 SELECT。**
>
> 绝对禁止：
>
> ```text
> INSERT
> UPDATE
> DELETE
> DROP
> ALTER
> TRUNCATE
> CREATE
> ```
>
> 建议限制：
>
> ```text
> SELECT ... LIMIT 100
> ```
>
> 禁止：
>
> ```text
> SELECT *
> FROM huge_table
> ```
>
> 造成全表扫描。
>
> 必要时自动增加合理 LIMIT。
>
> ------
>
> # 十二、数据库配置
>
> 使用环境变量：
>
> ```env
> MOM_DB_HOST=
> MOM_DB_PORT=
> MOM_DB_NAME=
> MOM_DB_USER=
> MOM_DB_PASSWORD=
> ```
>
> 如果当前项目已经存在数据库配置，优先复用。
>
> 数据库连接必须使用连接池。
>
> 不要每次查询重新创建数据库连接。
>
> ------
>
> # 十三、数据库工具的安全要求
>
> Agent 生成 SQL 后：
>
> Python 后端必须再次检查 SQL。
>
> 不要相信 LLM。
>
> 例如：
>
> ```python
> validate_sql(sql)
> ```
>
> 必须确保：
>
> ```text
> 只允许 SELECT
> ```
>
> 并且禁止：
>
> ```text
> ;
> INSERT
> UPDATE
> DELETE
> DROP
> ALTER
> TRUNCATE
> CREATE
> GRANT
> REVOKE
> ```
>
> 如果发现危险 SQL：
>
> 直接拒绝执行。
>
> ------
>
> # 十四、Agent Tool 结构
>
> 建议：
>
> ```text
> app/
> ├── agent/
> │   ├── agent.py
> │   ├── prompt.py
> │   └── tool_registry.py
> │
> ├── tools/
> │   ├── business_search.py
> │   ├── operation_search.py
> │   ├── code_search.py
> │   ├── file_reader.py
> │   └── database_query.py
> │
> ├── services/
> │   └── ai_client.py
> │
> └── ...
> ```
>
> 具体结构根据当前项目调整。
>
> ------
>
> # 十五、Tool 必须返回结构化结果
>
> 不要直接把大量文本塞给 AI。
>
> 例如：
>
> ```json
> {
>   "success": true,
>   "results": [
>     {
>       "file": "src/xxx/MesWorkOrderService.java",
>       "line": 128,
>       "content": "..."
>     }
>   ]
> }
> ```
>
> 业务知识也类似：
>
> ```json
> {
>   "success": true,
>   "documents": [
>     {
>       "title": "生产工单报工流程",
>       "path": "business/work_order/report.md",
>       "content": "..."
>     }
>   ]
> }
> ```
>
> 这样 Agent 更容易理解。
>
> ------
>
> # 十六、Agent 的核心逻辑
>
> 不要写成：
>
> ```text
> 每个问题都搜索代码
> ```
>
> 应该让 LLM 自己判断。
>
> 例如：
>
> 用户：
>
> ```text
> 生产工单怎么创建？
> ```
>
> Agent：
>
> ```text
> search_operation_guide
> ```
>
> ------
>
> 用户：
>
> ```text
> 为什么这个工单不能报工？
> ```
>
> Agent：
>
> ```text
> 1. 获取当前工单
> 2. 查询业务数据
> 3. 搜索报工业务规则
> 4. 搜索相关代码
> 5. 综合分析
> ```
>
> ------
>
> 用户：
>
> ```text
> 报工数量为什么不能大于工单数量？
> ```
>
> Agent：
>
> ```text
> search_code
> → read_code_file
> ```
>
> ------
>
> 用户：
>
> ```text
> 点击投产以后系统做了什么？
> ```
>
> Agent：
>
> ```text
> search_business_knowledge
> +
> search_code
> +
> read_code_file
> ```
>
> ------
>
> # 十七、Agent System Prompt
>
> 增加专门的 MOM Agent System Prompt。
>
> 核心原则：
>
> ```text
> 你是 MOM 系统智能助手。
> 
> 你的任务是帮助用户理解 MOM 系统的：
> 
> 1. 业务流程
> 2. 操作步骤
> 3. 操作结果
> 4. 业务规则
> 5. 系统校验
> 6. 代码实现
> 7. 当前业务数据
> 
> 回答问题时优先使用 MOM 提供的工具获取真实信息。
> 
> 不要凭经验猜测 MOM 的业务规则。
> 
> 如果业务文档和代码存在冲突：
> 
> 优先指出冲突，并分别说明来源。
> 
> 如果无法找到依据：
> 
> 明确告诉用户无法从当前 MOM 知识中确认。
> 
> 不要把猜测说成事实。
> 
> 回答普通业务用户时：
> 优先使用业务语言，不要大量展示代码。
> 
> 用户明确询问代码时：
> 可以展示相关代码、类、方法和文件位置。
> 
> 如果用户询问当前单据：
> 优先使用当前页面和业务对象上下文。
> 
> 如果查询数据库：
> 必须使用只读查询。
> 
> 不要执行任何修改数据的操作。
> ```
>
> ------
>
> # 十八、增加“回答依据”
>
> AI 最终回答最好能够告诉用户依据来自哪里。
>
> 例如：
>
> ```text
> 根据当前 MOM 系统实现：
> 
> 这个工单不能再次领料，是因为系统会检查该工单是否已经存在有效领料记录。
> 
> 依据：
> - 业务规则：领料规则
> - 代码：AymWmsIssueNoteServiceImpl.java
> - 方法：createIssueNote()
> ```
>
> 不要强制所有回答都展示来源。
>
> 但是涉及：
>
> - 代码规则
> - 校验
> - 系统行为
> - 数据结果
>
> 时，尽量提供依据。
>
> ------
>
> # 十九、当前页面上下文
>
> 预留接口：
>
> ```text
> POST /api/chat
> ```
>
> 请求可以增加：
>
> ```json
> {
>   "messages": [],
>   "context": {
>     "page": {
>       "module": "生产管理",
>       "menu": "生产工单",
>       "page": "工单详情"
>     },
>     "business": {
>       "type": "WORK_ORDER",
>       "id": "xxx",
>       "code": "WO20260918001"
>     }
>   }
> }
> ```
>
> 第一阶段前端如果还没有 MOM 页面集成：
>
> 可以允许 context 为空。
>
> 但后端和 Agent 架构必须预留这个能力。
>
> ------
>
> # 二十、未来 MOM 集成方式
>
> 当前：
>
> ```text
> MOM
>  ↓
> Chat UI
> ```
>
> 未来：
>
> ```text
> MOM Web
>  │
>  ├── 页面
>  │
>  └── AI 助手
>         ↓
>       MOM AI
>         ↓
>        Agent
> ```
>
> 用户打开：
>
> ```text
> 生产工单 WO20260918001
> ```
>
> AI 自动知道：
>
> ```text
> 当前模块：生产管理
> 当前页面：生产工单
> 当前工单：WO20260918001
> ```
>
> 用户直接问：
>
> ```text
> 为什么不能报工？
> ```
>
> 不需要再次告诉 AI 工单号。
>
> ------
>
> # 二十一、第一阶段重点支持的问题
>
> 开发完成后，至少测试以下问题：
>
> ### 操作类
>
> ```text
> 生产工单怎么创建？
> 生产工单怎么投产？
> 怎么进行报工？
> 怎么进行领料？
> 怎么进行退料？
> ```
>
> ### 流程类
>
> ```text
> 生产工单创建之后会发生什么？
> 投产之后下一步是什么？
> 报工之后会发生什么？
> 完工之后会发生什么？
> ```
>
> ### 规则类
>
> ```text
> 为什么不能再次领料？
> 报工数量有什么限制？
> 什么情况下不能完工？
> 什么情况下不能入库？
> ```
>
> ### 代码类
>
> ```text
> 报工数量校验在哪里？
> 领料校验在哪里？
> 这个接口对应哪个 Controller？
> 这个业务规则在哪个 Service 实现？
> ```
>
> ### 数据类
>
> ```text
> 这个工单现在是什么状态？
> 为什么这个工单不能报工？
> 这个工单有没有领料记录？
> ```
>
> ------
>
> # 二十二、日志
>
> Agent Tool 调用必须有日志。
>
> 例如：
>
> ```text
> [Agent] User question: 为什么不能报工？
> 
> [Agent] Tool: search_business_knowledge
> [Agent] Result: 2 documents
> 
> [Agent] Tool: query_database
> [Agent] SQL: SELECT ...
> 
> [Agent] Tool: search_code
> [Agent] Result: 4 files
> 
> [Agent] Tool: read_code_file
> [Agent] File: xxxServiceImpl.java
> 
> [Agent] Final answer generated
> ```
>
> 但：
>
> **不要在日志中打印 API Key、数据库密码等敏感信息。**
>
> ------
>
> # 二十三、第一阶段不要做的事情
>
> 不要：
>
> 1. 重写现有 Chat UI
> 2. 更换当前 AI API
> 3. 删除现有 API Key 配置
> 4. 引入复杂向量数据库
> 5. 一次性索引整个 MOM 项目
> 6. 把整个 MOM 源代码塞给 LLM
> 7. 让 AI 自动修改 MOM 代码
> 8. 让 AI 执行 UPDATE / DELETE
> 9. 让 AI 自己猜业务规则
> 10. 为了“看起来很 AI”而增加大量无意义功能
>
> ------
>
> # 二十四、测试要求
>
> 完成后必须实际运行项目。
>
> 测试：
>
> ```text
> 1. 原有聊天功能正常
> 2. API Key 正常
> 3. SSE 正常
> 4. Agent 正常
> 5. Tool Calling 正常
> 6. 业务知识搜索正常
> 7. 代码搜索正常
> 8. 文件读取正常
> 9. 数据库查询正常
> 10. SQL 安全检查正常
> 11. Tool 异常不会导致 Chat 崩溃
> 12. Agent 找不到答案时不会胡编
> ```
>
> 如果当前没有真实 MOM 数据库或代码目录：
>
> 不要伪造测试结果。
>
> 可以先实现：
>
> ```text
> MOM_PROJECT_ROOT
> MOM_DB_*
> MOM_KNOWLEDGE_ROOT
> ```
>
> 这些配置。
>
> ------
>
> # 二十五、配置
>
> 增加：
>
> ```env
> MOM_PROJECT_ROOT=/path/to/mom
> MOM_KNOWLEDGE_ROOT=/path/to/mom_knowledge
> 
> MOM_DB_HOST=
> MOM_DB_PORT=
> MOM_DB_NAME=
> MOM_DB_USER=
> MOM_DB_PASSWORD=
> ```
>
> 路径全部通过配置管理。
>
> 不要硬编码 Windows/Linux 路径。
>
> ------
>
> # 二十六、最终目标
>
> 最终我要得到：
>
> ```text
>                     MOM AI
>                        │
>                        ↓
>                     Agent
>                        │
>           ┌────────────┼────────────┐
>           ↓            ↓            ↓
>        业务知识       MOM代码      MOM数据库
>           │            │            │
>        操作流程      校验逻辑      实际数据
>        业务规则      API逻辑
>        流程说明      Service
>           │            │            │
>           └────────────┼────────────┘
>                        ↓
>                       LLM
>                        ↓
>                   用户可以理解的回答
> ```
>
> 重点不是做一个“会聊天的 AI”。
>
> 而是：
>
> **让 AI 成为 MOM 系统的使用说明书 + 业务流程助手 + 系统规则解释器。**
>
> 现在开始。
>
> 第一步：
> **只分析当前 Python Chat 项目，不修改代码。**
>
> 分析完成后输出：
>
> 1. 当前项目结构
> 2. 当前 AI API 调用位置
> 3. 当前 API Key 配置方式
> 4. 当前 SSE 实现方式
> 5. 当前 Chat UI 结构
> 6. 适合增加 Agent 的位置
> 7. 建议新增哪些文件
> 8. 第一阶段实施计划
>
> 然后再开始编码。